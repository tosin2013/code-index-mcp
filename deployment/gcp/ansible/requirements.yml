---
# Ansible Collections Required for Code Index MCP Deployment

collections:
  - name: google.cloud
    version: ">=1.0.0"

  - name: community.general
    version: ">=7.0.0"  # Supports latest features with ansible-core 2.16+

  - name: community.docker
    version: ">=3.0.0"

  - name: ansible.posix
    version: ">=1.0.0"
