---
# Ansible Collections Required for Code Index MCP Deployment

collections:
  - name: google.cloud
    version: ">=1.0.0"

  - name: community.docker
    version: ">=3.0.0"

  - name: ansible.posix
    version: ">=1.0.0"
